static constexpr float STEP_SIZE = 0.01f;

// This function draws a line from (x0, y0) to (x1, y1) on the frameBuffer using the specified color.
void line(int x0, int y0, int x1, int y1, TGAImage& frameBuffer, TGAColor colorToDrawLine);